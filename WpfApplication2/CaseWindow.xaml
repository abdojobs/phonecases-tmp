<Window x:Class="WpfApplication2.CaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CaseWindow" Height="400" Width="800">
    <!--MAIN GRID-->
    <Grid Background="#FF2B2B2B">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140*"/>
            <ColumnDefinition Width="60*"/>
        </Grid.ColumnDefinitions>
        
        <!-- MAIN TEXTBOX-->
        <TextBox AcceptsReturn="True" Margin="5"></TextBox>
        
        <!-- RIGHT STACKPANEL-->
        <StackPanel Grid.Column="1" Margin="5">
            <Expander Header="Ärendeinformation" Foreground="AliceBlue">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Användare:" Grid.Column="0" Grid.Row="0"/>
                    <TextBox HorizontalAlignment="Stretch"  Margin="5,0,0,5" Grid.Column="1" Grid.Row="0"/>


                </Grid>
            </Expander>
        </StackPanel>
        
    </Grid>
</Window>
